# CLAUDE.md

## 关于我
- **身份**：工业软件研发工程师，负责基于 FreeCAD 源码进行二次开发与界面定制。
- **技术栈**：
  - **语言**：C++（主要），偶尔接触 Python 绑定。
  - **框架**：Qt 4.8（Widgets、QSS 样式表、Designer）。
  - **构建系统**：CMake 3.27 + Visual Studio 2013（VC12）的 MSVC 工具链。
  - **版本控制**：Git / GitHub Desktop（新手阶段，偏向图形化操作）。
- **当前阶段**：正在深入理解 FreeCAD 的架构（App/Gui 分离、Workbench 机制、Command 系统、Ribbon 界面重构），并着手定制界面布局与样式。

## 回答偏好
- **语言与格式**：
  - 全程使用 **中文** 交流，技术术语可附英文原名。
  - 解释复杂概念时，**先给出一句话核心总结**，再用 bullet point 或表格展开。
  - 对代码的解释要 **逐行拆解**，说明每一行的 **语法含义** 和 **在该工程中的实际作用**。
  - 对于代码的结构或者界面解析，尽量采用图形化解释方法，或者函数调用链的展示。
- **风格与深度**：
  - **由浅入深**，不预设读者已掌握所有 C++/Qt 高级特性（如模板、运算符重载、Pimpl 惯用法等），必要时先补基础。
  - 多使用 **表格** 进行对比（如不同设计方案、相关配置项、VS 调试快捷键等）。
  - 提供 **具体的、可操作的分步指导**，优先给出 **Visual Studio 图形界面操作**，命令行作为备选。
  - 为抽象概念提供 **生活中的类比** 或 **记忆口诀**。
- **输出形式**：
  - 当分析代码段时，按 **“整体作用 → 逐行拆解 → 关联上下文 → 一句话总结”** 的结构组织。
  - 当解答报错或问题时，按 **“错误原因 → 解决方案 → 分步操作”** 的结构组织。
  - 对于 UI 调整，优先说明 **如何在 Qt Designer 中可视化修改**，再辅以必要的 XML 或代码修改。

## 当前项目与目标
### 项目概况
- **基于 FreeCAD 的工业软件二次开发**：
  - 在官方源码基础上，通过 CMake + VS2013 构建，链接自定义 LibPack 依赖库。
  - 引入了自研的 **Ribbon 式界面**（`PICRibbonButtonGroup`、`MainWindowDef`、`Ribbon` 控件），替换原生菜单栏和工具栏。
  - 将 FreeCAD 标准命令（`Std_New`、`Std_Open` 等）按照自定义分组（“开始”、“建模”、“物理设置”、“后处理”等）重新组织到标签页中。
  - 定制了 **QSS 样式表**（`New_UI.qss`）来美化整体界面。
  - 使用 **GitHub Desktop** 管理本地源码版本，并推送到远程仓库备份。

### 当前工作目标
1.  **理解 FreeCAD GUI 框架**：
    - 掌握 `Workbench` → `StdWorkbench` 继承体系，以及 `setupMenuBar`、`setupToolBars` 等虚函数的作用。
    - 理清 `Command` 系统和 `CommandManager` 如何管理可执行动作。
    - 明白 `MainWindow` 的构造与初始化流程（Pimpl、单例、MDI 区域、停靠窗口、定时器等）。
2.  **熟练使用 VS 2013 调试与代码浏览工具**：
    - 断点、逐过程/逐语句、调用堆栈、变量监视、对象浏览器、类视图。
    - 使用“查看调用层次结构”和“查找所有引用”分析函数关系。
3.  **安全地进行界面定制与代码修改**：
    - 修改 `ToolBarItem` 分组逻辑，调整 Ribbon 标签页的内容与顺序。
    - 调整 `.ui` 文件布局（如移动组名标签到按钮网格上方）。
    - 更新 QSS 样式，通过 `Gui/Stylesheets/` 或代码加载。
    - 每次修改前确保有 Git 提交点，方便快速回退。
4.  **逐步提升 C++ 工程能力**：
    - 在代码阅读中吃透 `virtual`、`const`、运算符重载、模板（`<>`）、智能指针（`Handle`）、`auto`、`static` 等关键特性在大型项目中的实际运用。
    - 能够独立从源码生成 Doxygen 文档，辅助理解类关系。

### 工程路径约定
- 源代码根目录：`D:\codeagent\FreeCAD`
- CMake 构建目录：`D:\codeagent\FreeCAD\build`
- LibPack 依赖库：`D:\codeagent\FreeCADLibs_11.5.3_x64_VC12`

## 回答时的特别注意事项
- 如果涉及到 FreeCAD 特有的宏（如 `TYPESYSTEM_HEADER`、`DEF_STD_CMD`）或工具类（`ParameterGrp::handle`），应先简要说明其设计目的再解释具体代码。