require "math"
--重写init
function init(name,max,mini,count)
	print("reset init(name,max,min,count)");
	--local var={};
	--local v={};
	--对应的增量
	--var.vit={};
	--var.pgt=0;
	--var.pit={};
	--var.pitF={};
	--for i=0,count,1
	--do
		--生成参数
		--v[i]=randmoFloat(mini,max)
		--初始化参数的增量
		--local vitV=math.abs(min-max)/2;
		--var.vit[i]=randmoFloat(-vitV,vitV);
		--var.pitF[i]=0;
		--var.pit[i]=0;
	--end
	--var.name=name;
	--var.variate=v;
	--var.max=max;
	--var.mini=mini;
	--调用C代码
	--addVariate(var.name,var.variate);
end